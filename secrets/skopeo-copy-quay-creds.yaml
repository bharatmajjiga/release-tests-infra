apiVersion: v1
kind: Secret
metadata:
  name: skopeo-copy-quay-creds
  namespace: pipelines-ci
  annotations:
    tekton.dev/docker-0: https://quay.io
type: kubernetes.io/basic-auth
stringData:
  username: "$QUAY_USER"
  password: "$QUAY_PASS"